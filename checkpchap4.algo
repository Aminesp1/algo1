PROCEDURE palyndrom(word:STRING,left,right:INTEGER)

BEGIN
   IF (word.length<=1) THEN
    write("this word is a palyndrom")
ELSE
    IF (word[left]<>word[right]) THEN
       write("this word is not a plyndrom")
    ELSE
     palyndrom(word,left+1,right-1) ;
END_IF

END